import './Header.css'
function Header(){

    return (
        <header class="top-header">
            <nav>
                    <a href="/">Home</a>
                    <a href="#">Projects</a>
                    <a href="#">Contact</a>
                    <a href="#">blogs</a>
            </nav>
        </header>
    );
}

export default Header